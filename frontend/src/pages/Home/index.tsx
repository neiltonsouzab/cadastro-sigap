import React from 'react';

// import { Container } from './styles';

const Home: React.FC = () => {
  return (
    <div
      style={{
        width: '100%',
        height: '100%',
        color: 'blue',
      }}
    />
  );
};

export default Home;
